diff --git a/dist/index.cjs b/dist/index.cjs
index 1f22ddcec5f837fbc1eff0c7bb215f261a83f0b7..65baefa980d4065c9214736db869630d092957e1 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -1629,7 +1629,7 @@ var Queue = class {
 	async addRepeatingJob(opts) {
 		if (!opts.repeat) throw new Error("Repeat options are required for repeating jobs");
 		const now = Date.now();
-		const repeatKey = `${opts.groupId}:${JSON.stringify(opts.repeat)}:${now}:${Math.random().toString(36).slice(2)}`;
+		const repeatKey = `${opts.groupId}:${now}:${Math.random().toString(36).slice(2)}`;
 		let nextRunTime;
 		if ("every" in opts.repeat) nextRunTime = now + opts.repeat.every;
 		else nextRunTime = this.getNextCronTime(opts.repeat.pattern, now);
@@ -1646,7 +1646,7 @@ var Queue = class {
 		const repeatJobKey = `${this.ns}:repeat:${repeatKey}`;
 		await this.r.set(repeatJobKey, JSON.stringify(repeatJobData));
 		await this.r.zadd(`${this.ns}:repeat:schedule`, nextRunTime, repeatKey);
-		const lookupKey = `${this.ns}:repeat:lookup:${opts.groupId}:${JSON.stringify(opts.repeat)}`;
+		const lookupKey = `${this.ns}:repeat:lookup:${opts.groupId}`;
 		await this.r.set(lookupKey, repeatKey);
 		const repeatId = `repeat:${repeatKey}`;
 		const jobHashKey = `${this.ns}:job:${repeatId}`;
@@ -1668,9 +1668,9 @@ var Queue = class {
 	/**
 	* Remove a repeating job
 	*/
-	async removeRepeatingJob(groupId, repeat) {
+	async removeRepeatingJob(groupId) {
 		try {
-			const lookupKey = `${this.ns}:repeat:lookup:${groupId}:${JSON.stringify(repeat)}`;
+			const lookupKey = `${this.ns}:repeat:lookup:${groupId}`;
 			const repeatKey = await this.r.get(lookupKey);
 			if (!repeatKey) return false;
 			const repeatJobKey = `${this.ns}:repeat:${repeatKey}`;
diff --git a/dist/index.d.cts b/dist/index.d.cts
index 99db2e6c3a376f19deaf3b6ada65edec8ef1f8e6..b43fd5ba59754ca3305075ff07dcf68e201eacf9 100644
--- a/dist/index.d.cts
+++ b/dist/index.d.cts
@@ -774,7 +774,7 @@ declare class Queue<T = any> {
   /**
    * Remove a repeating job
    */
-  removeRepeatingJob(groupId: string, repeat: RepeatOptions): Promise<boolean>;
+  removeRepeatingJob(groupId: string): Promise<boolean>;
 }
 //#endregion
 //#region src/adapters/groupmq-bullboard-adapter.d.ts
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 1e0c1c3db91c37d99fdd9f1eb71dae8dbb84743f..e383e4b40911397413f3156e62be16d4ced6bd96 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -774,7 +774,7 @@ declare class Queue<T = any> {
   /**
    * Remove a repeating job
    */
-  removeRepeatingJob(groupId: string, repeat: RepeatOptions): Promise<boolean>;
+  removeRepeatingJob(groupId: string): Promise<boolean>;
 }
 //#endregion
 //#region src/adapters/groupmq-bullboard-adapter.d.ts
diff --git a/dist/index.js b/dist/index.js
index 0b885b8c89cf49d0899ded6fe44b42880b4f749f..2c429f38b23e046aaaebbb261b6c9016004796ce 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1624,7 +1624,7 @@ var Queue = class {
 	async addRepeatingJob(opts) {
 		if (!opts.repeat) throw new Error("Repeat options are required for repeating jobs");
 		const now = Date.now();
-		const repeatKey = `${opts.groupId}:${JSON.stringify(opts.repeat)}:${now}:${Math.random().toString(36).slice(2)}`;
+		const repeatKey = `${opts.groupId}:${now}:${Math.random().toString(36).slice(2)}`;
 		let nextRunTime;
 		if ("every" in opts.repeat) nextRunTime = now + opts.repeat.every;
 		else nextRunTime = this.getNextCronTime(opts.repeat.pattern, now);
@@ -1641,7 +1641,7 @@ var Queue = class {
 		const repeatJobKey = `${this.ns}:repeat:${repeatKey}`;
 		await this.r.set(repeatJobKey, JSON.stringify(repeatJobData));
 		await this.r.zadd(`${this.ns}:repeat:schedule`, nextRunTime, repeatKey);
-		const lookupKey = `${this.ns}:repeat:lookup:${opts.groupId}:${JSON.stringify(opts.repeat)}`;
+		const lookupKey = `${this.ns}:repeat:lookup:${opts.groupId}`;
 		await this.r.set(lookupKey, repeatKey);
 		const repeatId = `repeat:${repeatKey}`;
 		const jobHashKey = `${this.ns}:job:${repeatId}`;
@@ -1663,9 +1663,9 @@ var Queue = class {
 	/**
 	* Remove a repeating job
 	*/
-	async removeRepeatingJob(groupId, repeat) {
+	async removeRepeatingJob(groupId) {
 		try {
-			const lookupKey = `${this.ns}:repeat:lookup:${groupId}:${JSON.stringify(repeat)}`;
+			const lookupKey = `${this.ns}:repeat:lookup:${groupId}`;
 			const repeatKey = await this.r.get(lookupKey);
 			if (!repeatKey) return false;
 			const repeatJobKey = `${this.ns}:repeat:${repeatKey}`;
diff --git a/dist/lua/reserve-batch.lua b/dist/lua/reserve-batch.lua
index 095536b0120d685ea39b6809cf19c97dd1639b0a..550e2ef0542d64ada75aa72ad7f5d58aa7a3b89d 100644
--- a/dist/lua/reserve-batch.lua
+++ b/dist/lua/reserve-batch.lua
@@ -45,6 +45,9 @@ if (now - lastCheck) >= stalledCheckInterval then
             local headScore = tonumber(head[2])
             redis.call("ZADD", readyKey, headScore, gid)
           end
+          -- Remove from group active list (BullMQ-style) - CRITICAL to unblock the group
+          local groupActiveKey = ns .. ":g:" .. gid .. ":active"
+          redis.call("LREM", groupActiveKey, 1, jobId)
           redis.call("DEL", ns .. ":lock:" .. gid)
           redis.call("DEL", procKey)
           redis.call("ZREM", processingKey, jobId)
